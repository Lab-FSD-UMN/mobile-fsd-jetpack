<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph">

    <activity android:id="@+id/mainActivity"
        android:name="your.package.name.MainActivity">
        <action android:id="@+id/actionToLoginActivity"
            app:destination="@id/loginActivity"/>
    </activity>

    <activity android:id="@+id/loginActivity"
        android:name="your.package.name.LoginActivity"/>

</navigation>