<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph">

    <activity android:id="@+id/loginActivity"
        android:name="com.example.mobile_fsd_jetpack.LoginActivity">
        <action android:id="@+id/actionToMainActivity"
            app:destination="@id/mainActivity"/>
    </activity>


    <activity android:id="@+id/mainActivity"
        android:name="com.example.mobile_fsd_jetpack.MainActivity">
        <action android:id="@+id/actionToLoginActivity"
            app:destination="@id/loginActivity"/>
    </activity>

</navigation>